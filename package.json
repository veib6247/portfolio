{
  "name": "portfolio",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "check": "astro check",
    "verify:search": "bun run verify:search:production && bun run verify:search:preview && bun run build",
    "verify:search:production": "bun run build && node scripts/validate-search.mjs",
    "verify:search:preview": "VERCEL_ENV=preview bun run build && VERCEL_ENV=preview node scripts/validate-search.mjs",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "@fontsource-variable/archivo": "^5.3.0",
    "@fontsource/barlow-condensed": "5.3.0",
    "@vercel/analytics": "^2.0.1",
    "@vercel/speed-insights": "^2.0.0",
    "astro": "^7.2.2"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "typescript": "6.0.3"
  }
}